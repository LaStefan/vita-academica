rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all website assets
    match /websites/styles.css {
      allow read;
    }
    match /websites/{userId}/index.html {
      allow read;
    }
  }
}